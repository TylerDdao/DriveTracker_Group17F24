<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Project1.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Project1"
    Shell.FlyoutBehavior="Disabled"
    Shell.TitleColor="red"
    Title="Project1">

    <ShellContent 
        Title="Login" 
        ContentTemplate="{DataTemplate local:LoginPage}" 
        Route="LoginPage"/>

    <ShellContent 
        Title="Account" 
        ContentTemplate="{DataTemplate local:AccountPage}" 
        Route="AccountPage"/>

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage"/>

    <ShellContent 
        Title="Trip Summary" 
        ContentTemplate="{DataTemplate local:TripSummaryPage}" 
        Route="TripSummaryPage"/>
    <ShellContent 
    Title="Trip History" 
    ContentTemplate="{DataTemplate local:TripHistoryPage}" 
    Route="TripHistoryPage"/>
</Shell>
